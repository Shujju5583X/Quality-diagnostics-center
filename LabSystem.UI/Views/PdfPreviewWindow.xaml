<Window x:Class="LabSystem.UI.Views.PdfPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Title="PDF Preview" Height="600" Width="800" WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <WebBrowser x:Name="PdfWebViewer" Grid.Row="0" Margin="10" />
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <ComboBox x:Name="LetterheadComboBox" Width="180" Height="40" Margin="0,0,10,0" 
                      VerticalContentAlignment="Center" HorizontalContentAlignment="Left"
                      SelectionChanged="LetterheadComboBox_SelectionChanged">
                <ComboBoxItem Content="With Letterhead" IsSelected="True" />
                <ComboBoxItem Content="Without Letterhead" />
            </ComboBox>
            <Button Content="CLOSE" Click="Close_Click" Margin="0,0,10,0" Width="120" Height="40" Style="{StaticResource MaterialDesignOutlinedButton}" />
            <Button Content="SAVE AS PDF" Click="SavePdf_Click" Margin="0,0,10,0" Width="150" Height="40" Style="{StaticResource MaterialDesignOutlinedButton}" />
            <Button Content="PRINT DIRECTLY" Click="PrintPdf_Click" Width="150" Height="40" Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>
    </Grid>
</Window>
