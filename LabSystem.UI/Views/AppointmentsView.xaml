<UserControl x:Class="LabSystem.UI.Views.AppointmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1200">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Style="{StaticResource MaterialDesignHeadline5TextBlock}" Text="Appointments" FontWeight="Bold" Margin="0 0 0 20"/>

        <!-- Date picker + New appointment button -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 0 0 16">
            <DatePicker SelectedDate="{Binding SelectedDate, Mode=TwoWay}" Width="180" materialDesign:HintAssist.Hint="Select Date"/>
            <TextBlock Text="{Binding StatusMessage, FallbackValue=''}" VerticalAlignment="Center" Margin="16 0 0 0" Foreground="Gray" FontStyle="Italic"/>
        </StackPanel>

        <!-- Split: left (appointments list) / right (booking form) -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="350"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Appointments list -->
            <materialDesign:Card Grid.Column="0" Padding="16" UniformCornerRadius="8" materialDesign:ShadowAssist.ShadowDepth="Depth2">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Appointments" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0 0 0 8"/>
                    <DataGrid Grid.Row="1" ItemsSource="{Binding Appointments}" AutoGenerateColumns="False" IsReadOnly="True"
                              SelectionMode="Single" materialDesign:DataGridAssist.CellPadding="8 4 8 4"
                              BorderThickness="0" RowHeaderWidth="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Time" Binding="{Binding AppointmentDate, StringFormat={}{0:hh:mm tt}}" Width="80"/>
                            <DataGridTextColumn Header="Patient" Binding="{Binding Patient.FullName}" Width="*"/>
                            <DataGridTextColumn Header="Purpose" Binding="{Binding Purpose}" Width="120"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80"/>
                            <DataGridTemplateColumn Header="Actions" Width="150">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Command="{Binding DataContext.CancelAppointmentCommand, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}" 
                                                    CommandParameter="{Binding}" Style="{StaticResource MaterialDesignFlatButton}" ToolTip="Cancel">
                                                <materialDesign:PackIcon Kind="Cancel" Width="16" Height="16" Foreground="Red"/>
                                            </Button>
                                            <Button Command="{Binding DataContext.MarkCompleteCommand, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}" 
                                                    CommandParameter="{Binding}" Style="{StaticResource MaterialDesignFlatButton}" ToolTip="Complete">
                                                <materialDesign:PackIcon Kind="CheckCircle" Width="16" Height="16" Foreground="Green"/>
                                            </Button>
                                            <Button Command="{Binding DataContext.MarkNoShowCommand, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}" 
                                                    CommandParameter="{Binding}" Style="{StaticResource MaterialDesignFlatButton}" ToolTip="No Show">
                                                <materialDesign:PackIcon Kind="AccountOff" Width="16" Height="16" Foreground="Gray"/>
                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </materialDesign:Card>

            <!-- Separator -->
            <GridSplitter Grid.Column="1" Width="4" Margin="8 0" HorizontalAlignment="Center" ShowsPreview="True"/>

            <!-- Right: Booking form -->
            <materialDesign:Card Grid.Column="2" Padding="16" UniformCornerRadius="8" materialDesign:ShadowAssist.ShadowDepth="Depth2">
                <StackPanel>
                    <TextBlock Text="Book Appointment" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0 0 0 16"/>

                    <TextBlock Text="Patient" Foreground="Gray" FontSize="12" Margin="0 0 0 4"/>
                    <ComboBox ItemsSource="{Binding Patients}" DisplayMemberPath="FullName" SelectedItem="{Binding SelectedPatientForBooking}"
                              materialDesign:HintAssist.Hint="Select Patient" Margin="0 0 0 12" IsTextSearchEnabled="True"/>

                    <TextBlock Text="Date" Foreground="Gray" FontSize="12" Margin="0 0 0 4"/>
                    <DatePicker SelectedDate="{Binding BookingDate, Mode=TwoWay}" Width="180" Margin="0 0 0 12"/>

                    <TextBlock Text="Time (HH:mm)" Foreground="Gray" FontSize="12" Margin="0 0 0 4"/>
                    <TextBox Text="{Binding BookingTime, Mode=TwoWay}" Width="100" Margin="0 0 0 12"/>

                    <TextBlock Text="Duration (minutes)" Foreground="Gray" FontSize="12" Margin="0 0 0 4"/>
                    <TextBox Text="{Binding DurationMinutes, Mode=TwoWay}" Width="80" Margin="0 0 0 12"/>

                    <TextBlock Text="Purpose" Foreground="Gray" FontSize="12" Margin="0 0 0 4"/>
                    <TextBox Text="{Binding Purpose, Mode=TwoWay}" Margin="0 0 0 12"/>

                    <TextBlock Text="Notes" Foreground="Gray" FontSize="12" Margin="0 0 0 4"/>
                    <TextBox Text="{Binding Notes, Mode=TwoWay}" TextWrapping="Wrap" AcceptsReturn="True" MinHeight="60" Margin="0 0 0 16"/>

                    <Button Command="{Binding BookAppointmentCommand}" Style="{StaticResource MaterialDesignRaisedAccentButton}" Height="42">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="CalendarPlus" Margin="0 0 8 0"/>
                            <TextBlock Text="BOOK APPOINTMENT" FontWeight="Bold"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </materialDesign:Card>
        </Grid>
    </Grid>
</UserControl>