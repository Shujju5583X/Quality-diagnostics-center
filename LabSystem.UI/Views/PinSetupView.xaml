<Window x:Class="LabSystem.UI.Views.PinSetupView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Set up PIN" Height="300" Width="400" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Welcome!" FontSize="20" FontWeight="Bold" Margin="0,0,0,10" Grid.Row="0"/>
        <TextBlock Text="Please set up a new PIN for your account." Margin="0,0,0,20" Grid.Row="1"/>

        <TextBlock Text="New PIN:" Grid.Row="2"/>
        <TextBox Text="{Binding NewPin, UpdateSourceTrigger=PropertyChanged}" Grid.Row="3" Margin="0,5,0,10" MaxLength="10"/>

        <TextBlock Text="Confirm PIN:" Grid.Row="4"/>
        <TextBox Text="{Binding ConfirmPin, UpdateSourceTrigger=PropertyChanged}" Grid.Row="5" Margin="0,5,0,10" MaxLength="10"/>

        <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Grid.Row="6" Margin="0,10,0,0" TextWrapping="Wrap"/>

        <Button Content="Save PIN" Command="{Binding SavePinCommand}" Grid.Row="6" VerticalAlignment="Bottom" HorizontalAlignment="Right" Width="100" Height="30"/>
    </Grid>
</Window>
