<Window x:Class="LabSystem.UI.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:LabSystem.UI.Views"
        Title="Technician Authentication - Quality Diagnostics Center" 
        Height="420" Width="380" 
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        FontFamily="{DynamicResource MaterialDesignFont}">
    
    <Window.Resources>
        <local:StringVisibilityConverter x:Key="StringVisibilityConverter" />
    </Window.Resources>
    <Border CornerRadius="12" Background="{DynamicResource MaterialDesignPaper}" BorderBrush="{DynamicResource PrimaryHueMidBrush}" BorderThickness="1.5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Gradient Header Banner -->
            <Border Grid.Row="0" CornerRadius="10,10,0,0" Height="100">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#673AB7" Offset="0" /> <!-- DeepPurple -->
                        <GradientStop Color="#9C27B0" Offset="1" /> <!-- Purple -->
                    </LinearGradientBrush>
                </Border.Background>
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="QUALITY DIAGNOSTICS" Foreground="White" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="Technician Authentication" Foreground="#E0E0E0" FontSize="13" FontWeight="SemiBold" Margin="0,4,0,0" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Form Body -->
            <Grid Grid.Row="1" Margin="24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Select Technician ComboBox -->
                <ComboBox Grid.Row="0" 
                          ItemsSource="{Binding StaffMembers}" 
                          SelectedItem="{Binding SelectedStaff}"
                          DisplayMemberPath="FullName"
                          Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                          materialDesign:HintAssist.Hint="Select Technician"
                          Margin="0,8,0,16" />

                <!-- PIN PasswordBox -->
                <PasswordBox Grid.Row="1" 
                             x:Name="PinBox"
                             PasswordChanged="PinBox_PasswordChanged"
                             Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"
                             materialDesign:HintAssist.Hint="Enter 4-Digit PIN"
                             Margin="0,0,0,16" />

                <!-- Error Message TextBlock -->
                <TextBlock Grid.Row="2" 
                           Text="{Binding ErrorMessage}" 
                           Foreground="#E53935" 
                           FontWeight="SemiBold"
                           TextWrapping="Wrap" 
                           HorizontalAlignment="Center"
                           TextAlignment="Center"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource StringVisibilityConverter}}"
                           Margin="0,0,0,12" />

                <!-- Action Buttons -->
                <StackPanel Grid.Row="4">
                    <Button Content="SIGN IN" 
                            Command="{Binding LoginCommand}"
                            Style="{StaticResource MaterialDesignRaisedButton}" 
                            Height="44" 
                            FontSize="15" 
                            FontWeight="Bold" 
                            materialDesign:ButtonAssist.CornerRadius="6"/>
                    
                    <Button Content="Exit Application" 
                            Click="Exit_Click"
                            Style="{StaticResource MaterialDesignFlatButton}" 
                            Foreground="{DynamicResource MaterialDesignBodyLight}"
                            Margin="0,8,0,0" 
                            Height="36" />
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
