<UserControl x:Class="LabSystem.UI.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:LabSystem.UI.Views"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <UserControl.Resources>
    </UserControl.Resources>
    <Border CornerRadius="12" Background="{DynamicResource MaterialDesignPaper}" BorderBrush="{StaticResource PrimaryBrush}" BorderThickness="1.5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Gradient Header Banner: Teal -> Cyan -->
            <Border Grid.Row="0" CornerRadius="10,10,0,0" Height="100"
                    Background="{StaticResource HeaderGradientBrush}">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="QUALITY DIAGNOSTICS" Foreground="White" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="Technician Authentication" Foreground="#B2DFDB" FontSize="13" FontWeight="SemiBold" Margin="0,4,0,0" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Form Body -->
            <Grid Grid.Row="1" Margin="24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Select Technician ComboBox -->
                <ComboBox Grid.Row="0"
                          ItemsSource="{Binding StaffMembers}"
                          SelectedItem="{Binding SelectedStaff}"
                          DisplayMemberPath="FullName"
                          Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                          materialDesign:HintAssist.Hint="Select Technician"
                          Margin="0,8,0,16" />

                <!-- PIN PasswordBox -->
                <PasswordBox Grid.Row="1"
                             x:Name="PinBox"
                             PasswordChanged="PinBox_PasswordChanged"
                             Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"
                             materialDesign:HintAssist.Hint="Enter 4-Digit PIN"
                             Margin="0,0,0,16" />

                <!-- Error Message TextBlock -->
                <TextBlock Grid.Row="2"
                           Text="{Binding ErrorMessage}"
                           Foreground="{StaticResource ErrorBrush}"
                           FontWeight="SemiBold"
                           TextWrapping="Wrap"
                           HorizontalAlignment="Center"
                           TextAlignment="Center"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource StringVisibility}}"
                           Margin="0,0,0,12" />

                <!-- Action Buttons -->
                <StackPanel Grid.Row="4">
                    <Button Content="SIGN IN"
                            Command="{Binding LoginCommand}"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Height="44"
                            FontSize="15" />

                    <Button Content="Exit Application"
                            Click="Exit_Click"
                            Style="{StaticResource MaterialDesignFlatButton}"
                            Foreground="{StaticResource TextSecondaryBrush}"
                            Margin="0,8,0,0"
                            Height="36" />
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</UserControl>
